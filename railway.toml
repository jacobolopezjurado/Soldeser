# Config Railway - raíz del monorepo
# https://docs.railway.app/reference/config-as-code

[build]
builder = "nixpacks"
# Nixpacks usa nixpacks.toml para los comandos

[deploy]
# Start definido en nixpacks.toml
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
